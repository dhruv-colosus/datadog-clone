datadog.techbrig.co {
    encode zstd gzip

    handle_path /api/* {
        reverse_proxy api:8000 {
            flush_interval -1
        }
    }

    handle {
        reverse_proxy web:3000
    }
}
